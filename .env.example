# 服务端口（默认 3007）
PORT=3007

# 时区设置（默认 Asia/Shanghai）
TZ=Asia/Shanghai

# Node 环境
NODE_ENV=production

# Web 面板端口（默认 3007）
# ADMIN_PORT=3007

# 管理员账号（仅首次启动生效，默认 admin/admin，建议部署后尽快修改）
# ADMIN_USER=admin
# ADMIN_PASS=admin

# 允许跨域来源（生产环境建议填写具体域名，多个用逗号分隔）
# ALLOWED_ORIGINS=http://localhost:3007
